# 无声之声 · 生产环境依赖（阿里云 ECS 部署用）
# CPU 推理：torch 请用下方 install 脚本中的 CPU 源安装

flask>=3.0.0
gunicorn>=21.2.0
opencv-python-headless>=4.8.0
mediapipe>=0.10.9
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
edge-tts>=6.1.0
pygame>=2.5.0

# PyTorch CPU（体积较大，install.sh 中单独安装）
# pip install torch --index-url https://download.pytorch.org/whl/cpu
